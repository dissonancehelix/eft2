# Reference media. Keep the path documented in README; do not push this
# without Git LFS or a smaller derived clip set.
/EFT Bloodbowl POV.mp4
*.mp4
*.mov
*.avi
*.mkv

# s&box reference tree. Keep the path documented in AGENTS.md; do not push
# the full s&box checkout unless the user deliberately promotes it.
/SBox/Docs/
/SBox/Source/
/SBox/Runtime/
/SBox/Bootstrap.bat
/SBox/README.md

# Prebuilt tool binaries. Tools/ffmpeg.exe is a holding spot until
# tools/observer/ is built; commit it only if you want it tracked.
/Tools/ffmpeg.exe

# Evidence/dev captures. Keep paths documented in README, but do not
# upload bulk captures unless they become curated fixtures.
/lua/game logs/
/Assets/Backgrounds/
/Assets/Screenshots/

# s&box compiled/generated outputs. Produced by the editor on open —
# do not track; they are regenerated automatically.
*.scene_c
*.scene_d
*.vtex_c
*.vmat_c
*.vmdl_c
*.vpcf_c
*.vsnd_c
*.vjs_c
*.vcss_c
*.vsndevts_c

# s&box local project cache and transient data.
Game/eft2/.sbox/transient/
Game/eft2/.sbox/cloud.db
Game/eft2/.sbox/cloud/

# Claude Code local project data (session state, not repo content).
.claude/

# Common build and editor outputs.
bin/
obj/
.vs/
.vscode/
.idea/
*.user
*.suo
*.tmp
*.temp
*.bak
*.cache
__pycache__/

# OS files.
Thumbs.db
Desktop.ini
.DS_Store

# Local workspace shortcuts (not repo artifacts).
# Editor.lnk opens sbox-dev.exe for Game/eft2/ — local only.
Editor.lnk
*.lnk
